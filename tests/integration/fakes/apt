#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
source "${SCRIPT_DIR}/_common.sh"

if [[ -n "${FAKE_APT_FAIL:-}" ]]; then
  echo "${FAKE_APT_FAIL}" >&2
  exit 1
fi

exit 0
